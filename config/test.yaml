# Image settings
image_width: 320
image_height: 240

# Camera settings
fov: 60.0
camera_position: [0.0, 0.15, 0.5]
camera_lookat: [0.0, 0.1, 0.0]

# Scene settings
mesh: "assets/bunny.obj"

# Rendering settings
samples_per_pixel: 4
max_depth: 4

# Output settings
output_file: "test_render.png"
gamma: 2.2
